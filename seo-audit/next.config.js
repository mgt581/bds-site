/** @type {import('next').NextConfig} */
const nextConfig = {
  // Allow the Next.js app to be served from a subpath if needed in future
  // basePath: '/audit-tool',
};

module.exports = nextConfig;
